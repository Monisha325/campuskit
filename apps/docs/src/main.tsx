// The public deployment uses the same real booking application as apps/web.
// Keeping one entry point prevents a static information page from drifting away
// from the authenticated booking experience.
import "../../web/src/main";
